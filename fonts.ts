// // app/fonts.ts
// import { Rubik } from "next/font/google";

// const rubik = Rubik({
//   subsets: ["latin"],
//   variable: "--font-rubik",
// });

// export const fonts = {
//   rubik,
// };

// app/fonts.ts

// app/fonts.ts

import { Inter } from "next/font/google";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const fonts = {
  inter,
};
