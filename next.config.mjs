/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "zngeyti3xb5dy8sj.public.blob.vercel-storage.com",
      "xsgames.co",
      "storage.googleapis.com",
      "api.typeform.com",
    ],
  },
};

export default nextConfig;
